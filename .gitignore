
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*

# Site build output and local agent tooling (plan Decision 10)
node_modules/
_site/
.claude/
.gc/
