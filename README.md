# Addable Labs — company website

Source for the Addable Labs website, published with GitHub Pages at
https://addable-labs.github.io/ (custom domain to follow).

This repository is the **pilot rig of the Addable Labs Factory**: it is built
and maintained by the factory's agents. All changes arrive as pull requests
from factory branches; `main` is protected and is deployed by GitHub Actions.

- Factory definition: `addable-labs/factory` (private)
- Status: empty — the first factory bead is "scaffold the site"
